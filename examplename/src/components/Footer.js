import React, { Component } from 'react';

class Footer extends Component {
    render() {
        return (
            <div>Click the 'U' button to update, click it again to fill the boxes with the old data.</div>
        )
    }
}

export default Footer;