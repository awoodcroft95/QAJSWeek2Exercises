import React, { Component } from 'react';

class Footer extends Component {
    render() {
        return (
            <div>Click the 'U' button to update.</div>
        )
    }
}

export default Footer;